name: Simple Test
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello from act!"
      
      - name: Test secret
        env:
          MY_SECRET: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          if [ -n "$MY_SECRET" ]; then
            echo "Secret length: ${#MY_SECRET}"
          else
            echo "No secret found"
          fi